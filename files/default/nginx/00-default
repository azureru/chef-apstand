# Production
# This nginx site definition will deny all
# VHOST CNAME domain parking -
# activate this as default and define your own VHOST on other file

# return 444 (no response)
server {
  listen 80 default_server;
  return 444;
}